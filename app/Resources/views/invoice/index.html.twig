{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper row3">
        <main class="hoc container clear"> 
            <div class="btmspace-50">
                <h2 class="heading">Lista de facturas</h2>

                <table>
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Usuario</th>
                            <th>Número</th>
                            <th>Path</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for invoice in invoices %}
                        <tr>
                            <td><a href="{{ path('invoice_edit', { 'id': invoice.id }) }}">{{ invoice.id }}</a></td>
                            <td>{{ invoice.user }}</td>
                            <td>{{ invoice.number }}</td>
                            <td>{{ invoice.path }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

            </div>
            <div class="clear"></div>
        </main>
    </div>
{% endblock %}
