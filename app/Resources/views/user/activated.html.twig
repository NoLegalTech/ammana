{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper row3">
        <main class="hoc container clear"> 
            <div class="btmspace-50 small-sheet">
                <p>
                    {{ i18n.es.activation_page.message }} <a href="{{ path('user_login') }}">{{ i18n.es.activation_page.login_page }}</a>
                </p>
            </div>
            <div class="clear"></div>
        </main>
    </div>

{% endblock %}
