{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper row3">
        <main class="hoc container clear"> 
            <div class="btmspace-50">
                <h2 class="heading">Lista de facturas</h2>

                <table>
                    <thead>
                        <tr>
                            <th>Fecha</th>
                            <th>Número</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for invoice in invoices %}
                        <tr>
                            <td>{{ invoice.emittedAt | date('d/m/Y') }}</td>
                            <td>{{ invoice.number }}</td>
                            {% if invoice.url %}
                                <td><a href="{{ invoice.url }}" target="_blank">Descargar factura</a></td>
                            {% else %}
                                <td>Factura no disponible</td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

            </div>
            <div class="clear"></div>
        </main>
    </div>
{% endblock %}
