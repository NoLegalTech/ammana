{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper row3">
        <main class="hoc container clear"> 
            <div class="btmspace-50 small-sheet">
                <h2 class="heading">Establecer nueva contraseña</h2>
                <p>
                    Introduce a continuación tu email y te enviaremos un correo para que puedas establecer una nueva contraseña.
                </p>
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input class="btn" type="submit" value="Enviar" />
                {{ form_end(form) }}
            </div>
            <div class="clear"></div>
        </main>
    </div>

{% endblock %}
